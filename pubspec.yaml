name: openclaw_mobile
description: OpenClaw Mobile with BrowserOS MCP Integration
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  provider: ^6.1.1
  web_socket_channel: ^2.4.0
  multicast_dns: ^0.3.2+6
  encrypt: ^5.0.3
  local_auth: ^2.1.8
  connectivity_plus: ^5.0.2
  uuid: ^4.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true